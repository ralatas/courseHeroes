import Authorization from '@/components/modals/authorization/authorization.vue'

export default {
    name: 'App',
    data: () => ({
        showAuth: false
    }),
    components: {
        Authorization
    },
}
